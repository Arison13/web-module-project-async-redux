const initialState = {
    name:"ARI",
    date:"Today",
    assignment:"Lambda project"
}

export const reducer = (state = initialState, action) => {
    switch(action.type){
        default:
            return state;
    }

 }